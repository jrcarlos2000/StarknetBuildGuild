import styled from "styled-components";

/**
 * Expanding whitespaces that occupy the space left at its fullest.
 * Utilize flex-grow so it'll work with display: flex only.
 */
export const Whitespaces = styled.div`
    flex-grow: 1;
`